naftiko: 1.0.0-alpha2
info:
  label: SAP BI Tools SAP Analytics Cloud API — Calendars
  description: 'SAP BI Tools SAP Analytics Cloud API — Calendars. 1 operations. Lead operation: SAP BI Tools List calendar
    events. Self-contained Naftiko capability covering one Sap Bi Tools business surface.'
  tags:
  - Sap Bi Tools
  - Calendars
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SAP_BI_TOOLS_API_KEY: SAP_BI_TOOLS_API_KEY
capability:
  consumes:
  - type: http
    namespace: sap-analytics-cloud-calendars
    baseUri: https://{tenant}.{datacenter}.sapanalytics.cloud
    description: SAP BI Tools SAP Analytics Cloud API — Calendars business capability. Self-contained, no shared references.
    resources:
    - name: api-v1-calendars
      path: /api/v1/calendars
      operations:
      - name: listcalendars
        method: GET
        description: SAP BI Tools List calendar events
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.SAP_BI_TOOLS_API_KEY}}'
  exposes:
  - type: rest
    namespace: sap-analytics-cloud-calendars-rest
    port: 8080
    description: REST adapter for SAP BI Tools SAP Analytics Cloud API — Calendars. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/api/v1/calendars
      name: api-v1-calendars
      description: REST surface for api-v1-calendars.
      operations:
      - method: GET
        name: listcalendars
        description: SAP BI Tools List calendar events
        call: sap-analytics-cloud-calendars.listcalendars
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: sap-analytics-cloud-calendars-mcp
    port: 9090
    transport: http
    description: MCP adapter for SAP BI Tools SAP Analytics Cloud API — Calendars. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: sap-bi-tools-list-calendar
      description: SAP BI Tools List calendar events
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: sap-analytics-cloud-calendars.listcalendars
      outputParameters:
      - type: object
        mapping: $.
